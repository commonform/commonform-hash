{
  "name": "commonform-hash",
  "description": "hash Common Forms",
  "version": "0.4.0",
  "author": "Kyle E. Mitchell <kyle@kemitchell.com> (http://kemitchell.com)",
  "bugs": "https://github.com/commonform/commonform-hash/issues",
  "dependencies": {
    "commonform-serialize": "^0.4.0",
    "sha256": "^0.2.0"
  },
  "devDependencies": {
    "chai": "^2.2.0",
    "check-versions": "1.x",
    "istanbul": "^0.3.5",
    "jscs": "^1.10.0",
    "jshint": "^2.6.0",
    "mocha": "^2.1.0",
    "semver": "^4.2.0"
  },
  "homepage": "https://commonform.github.io",
  "keywords": [
    "contracts",
    "digest",
    "document assembly",
    "hash",
    "knowledge management",
    "law"
  ],
  "license": "Apache-2.0",
  "main": "source/index",
  "repository": "commonform/commonform-hash",
  "scripts": {
    "check": "check-versions --check-module",
    "coverage": "istanbul cover _mocha && istanbul check-coverage coverage/coverage.json",
    "lint": "jshint source test && jscs source test",
    "pre-commit": "npm run check && npm run lint && npm run coverage",
    "test": "mocha"
  }
}
